# Backend API base URL used by the Vite dev server proxy and the built frontend.
# Leave empty (or unset) in production when the API is served on the same origin.
VITE_API_BASE_URL=http://localhost:8001
