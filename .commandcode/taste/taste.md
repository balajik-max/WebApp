- Prefers blink-free navigation with NO loading flashes/loaders/skeletons, and prefers INSTANT photo swaps (no transition animation) over cross-fades — explicitly rejects animated cross-fade transitions as undesirable and wants the prior instant-swap behavior restored. Confidence: 0.9
- Does NOT want camera rotation/spin animation during image transitions — the next image should appear at the exact same viewport (same zoom/rotation) via an instant swap with no spinning camera. Confidence: 0.9
- Avoids redundant/duplicate functionality — flags when new changes re-implement capabilities that already exist in the codebase. Confidence: 0.7
- Wants proactive preloading/prefetching of the next item in a sequence so transitions are instant (e.g. next photo prefetched into cache before it's needed). Confidence: 0.8
- Approves of and expects rigorous toolchain verification after changes: typecheck (`tsc --noEmit`), lint (`eslint`), and production build (`npm run build`) all run and must pass. Confidence: 0.8
- Expects fixes and features to be applied and verified uniformly across ALL trigger paths for a given action (e.g. manual prev/next buttons, keyboard arrows, on-photo overlay arrows, and slideshow auto-advance must all produce the same behavior); flags when a change only covers one path. Confidence: 0.8
- Rejects unrequested visual/behavioral additions (e.g. transition animations) when a targeted fix is requested — wants the previously-working behavior preserved and only the specific reported issue addressed; pushes back strongly against regressions. Confidence: 0.8
